{
  "name": "@pco/admin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'admin lint placeholder (Phase 1: next lint)'",
    "dev": "echo 'Phase 1: run next dev (Content Admin + Super Admin). See ADR-002/ADR-003.'"
  }
}
