{
  "name": "@pco/cms",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'cms lint ok'",
    "validate": "node scripts/validate-seed.mjs"
  }
}
